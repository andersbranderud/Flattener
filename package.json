{
  "name": "yourProject",
  "version": "0.1.0",
  "private": true,
  "description": "",
  "scripts": {
    "watch": "webpack --watch",
    "newinstall": "yarn install --modules-folder ./node_modules",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider; yarn build"
  },
  "devDependencies": {
    "ts-jest": "^29.0.3",
    "ts-loader": "^8.4.0",
    "typescript": "^4.8.4",
    "webpack": "^4.46.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "ts-node": "^10.9.1"
  },
  "jest": {
    "globals": {
      "ts-jest": {
        "tsconfig": "tsconfig.json"
      }
    },
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "^.+\\.(ts|tsx)$": "./node_modules/ts-jest/preprocessor.js"
    },
    "testMatch": [
      "**/tests/**/*.test.(ts|js)",
      "**/tests/*.test.(ts|js)"
    ],
    "testEnvironment": "node"
  }
}
